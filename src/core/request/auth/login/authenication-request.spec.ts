import { AuthenicationRequest } from './authenication-request';

describe('AuthenicationRequest', () => {
  it('should create an instance', () => {
    expect(new AuthenicationRequest()).toBeTruthy();
  });
});
